# 这些都很容易cd进去，就暂时不设变量了
# let $desktop = '~/Desktop'
# let $vimfiles = '~/vimfiles'
# 注：命令长度达到3字符的，会有自动neocomplete，2以下的则不会。
# 这应该是neocomplete的设定，而非bug
# commit相关的操作，还是使用fugitive吧:)
alias gs='git status'

# git clone不频繁使用，暂不alias
# alias gcl='git clone'

alias gh='git push'
alias gl='git pull'
alias gt='go test -run'

alias l='ls --color=auto -lrtahF'

